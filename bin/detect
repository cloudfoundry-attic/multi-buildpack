#!/usr/bin/env ruby

build_dir = ARGV[0]

multi_buildpack_yml = File.join(build_dir, 'multi-buildpack.yml')

if File.exist?(multi_buildpack_yml)
  puts 'Experimental multi-buildpack'
  exit 0
else
  exit 1
end
